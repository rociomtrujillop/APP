package com.example.appestafa.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta para el tema claro (Light Theme)
val PrimaryBlue = Color(0xFF1976D2)   // Un azul vibrante y accesible
val SecondaryTeal = Color(0xFF009688) // Un verde azulado (teal) para acentos
val AccentPink = Color(0xFFE91E63)    // Un rosa para destacar elementos importantes

// Paleta para el tema oscuro (Dark Theme)
val PrimaryBlueDark = Color(0xFF64B5F6)   // Un azul más suave para fondos oscuros
val SecondaryTealDark = Color(0xFF4DB6AC) // Un teal más claro
val AccentPinkDark = Color(0xFFF06292)    // Un rosa más suave
